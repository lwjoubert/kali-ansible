---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install packages
    package:
      name:
        - htop
        - tmux
        - vim-nox
        - python3-psutil

  - name: install Kerbrute
    shell: go install github.com/ropnop/kerbrute@master
